{% extends "default/layout.twig" %}

{% block title %}Dashboard - {{ app_name }}{% endblock %}

{% block styles %}
<link rel="stylesheet" href="{{ asset('assets/css/dashboard.css') }}?v=4">
{% endblock %}

{% block content %}
{% include "default/components/topbar.twig" %}

<!-- Sidebar -->
<aside class="sidebar" id="sidebar">
    <nav class="sidebar-nav">
        <div class="nav-section">
            <div class="nav-section-title">Menu Principal</div>
            <a class="nav-link active" href="{{ url('dashboard') }}">
                <i class="bi bi-speedometer2"></i> Dashboard
            </a>
        </div>
        <div class="nav-section">
            <div class="nav-section-title">Sistema</div>
            <a class="nav-link nav-dropdown" href="javascript:void(0)" onclick="toggleSubmenu(this)">
                <i class="bi bi-gear"></i> Configurações
                <i class="bi bi-chevron-down nav-arrow"></i>
            </a>
            <div class="nav-submenu">
                <a class="nav-link nav-sublink" href="{{ url('users') }}">
                    <i class="bi bi-people"></i> Usuários
                </a>
                <a class="nav-link nav-sublink" href="{{ url('status') }}">
                    <i class="bi bi-toggle-on"></i> Status
                </a>
                <a class="nav-link nav-sublink" href="{{ url('levels') }}">
                    <i class="bi bi-shield-lock"></i> Níveis de Acesso
                </a>
                <a class="nav-link nav-sublink" href="{{ url('genders') }}">
                    <i class="bi bi-person-badge"></i> Gêneros
                </a>
                <a class="nav-link nav-sublink" href="{{ url('settings') }}">
                    <i class="bi bi-sliders"></i> Geral
                </a>
            </div>
        </div>
    </nav>
</aside>

<!-- Main Content -->
<main class="main-content">
    <div class="page-header">
        <h1>Dashboard</h1>
        <p class="page-subtitle">Bem-vindo ao painel administrativo do Colégio São Gonçalo</p>
    </div>
</main>

{% include "default/components/scripts.twig" %}
{% endblock %}
